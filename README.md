# ⚔️ Pokémon Battle

Интерактивное веб-приложение на React, где два случайных покемона сражаются между собой.

## 🎮 Особенности

- **Случайные покемоны**: При загрузке отображаются два случайных покемона из PokéAPI
- **Битва**: Нажмите "Start Battle" чтобы начать битву
- **Анимации**: Визуальные эффекты при битве и победе
- **История битв**: Каждая битва автоматически сохраняется в JSON файл с полной информацией о покемонах
- **Статистика**: Просмотр истории всех битв на странице статистики
- **API сервер**: Все данные сохраняются в JSON файл через API сервер
- **Адаптивный дизайн**: Работает на всех устройствах
- **Loader**: Красивый индикатор загрузки

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

**⚠️ Важно: API сервер должен быть запущен для работы приложения!**

```bash
# В одном терминале запустите API сервер
npm run server

# В другом терминале запустите фронтенд
npm run dev

# Или запустите оба одновременно
npm run dev:full
```

Приложение откроется на `http://localhost:5173`
API сервер будет доступен на `http://localhost:3001`

Все битвы автоматически сохраняются в `src/data/battleStats.json` через API сервер.

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
pokemon-battle/
├── src/
│   ├── components/
│   │   ├── BattleArena.jsx      # Основная арена битвы
│   │   ├── PokemonCard.jsx      # Карточка покемона
│   │   ├── Loader.jsx           # Индикатор загрузки
│   │   └── Stats.jsx            # Компонент статистики
│   ├── services/
│   │   └── pokemonAPI.js        # API интеграция с PokéAPI
│   ├── App.jsx                  # Главный компонент
│   ├── main.jsx                 # Точка входа
│   └── index.css                # Глобальные стили
├── index.html
├── package.json
└── vite.config.js
```

## 🎯 Функциональность

### Основной функционал

1. **Загрузка покемонов**: При загрузке приложения автоматически загружаются два случайных покемона
2. **Отображение данных**: Показываются имя, изображение, типы и базовые характеристики (HP, Attack, Defense, Speed)
3. **Битва**: При нажатии "Start Battle" определяется победитель на основе характеристик и случайности
4. **Победитель**: Визуальное выделение победителя с анимацией
5. **Новая битва**: Кнопка "Try Again" загружает новых покемонов

### Дополнительные возможности

- ✅ Loader при загрузке данных
- ✅ Анимации появления покемонов
- ✅ История всех битв с полной информацией о покемонах
- ✅ Автоматическое сохранение каждой битвы в JSON файл через API
- ✅ Все данные хранятся в `src/data/battleStats.json`
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Визуальные эффекты (покачивание, вспышка при победе)

### Сохранение битв

Каждая битва автоматически сохраняется в JSON файл (`src/data/battleStats.json`) с полной информацией:
- Имена и ID покемонов
- Изображения покемонов
- Статистика (HP, Attack, Defense, Speed, Types)
- Победитель
- Мощность каждого покемона в битве
- Дата и время битвы

**Важно:** 
- Все данные хранятся только в JSON файле через API сервер
- localStorage не используется
- API сервер должен быть запущен для работы приложения

## 🛠 Технологии

- **React 18+** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **CSS Modules** - Стилизация компонентов
- **Fetch API** - Запросы к PokéAPI

## 📱 API

Приложение использует [PokéAPI](https://pokeapi.co/) для получения данных о покемонах.

## 🎨 Анимации

- Анимация появления покемонов (slide-in)
- Покачивание во время битвы (shake)
- Подсветка победителя (glow)
- Пульсация VS разделителя
- Анимация загрузки (вращающийся покебол)

## 📊 Статистика

Статистика сохраняется в JSON файле через API и включает:
- Общее количество битв
- Количество побед каждого покемона
- Топ победитель
- Полная история всех битв с деталями

## 📝 Лицензия

MIT

